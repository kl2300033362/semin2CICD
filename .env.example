# Example environment variables to run the application in Docker/CI
# Backend Spring datasource (example for MySQL)
SPRING_DATASOURCE_URL=jdbc:mysql://mysql:3306/tours_travel_system?createDatabaseIfNotExist=true&useUnicode=true
SPRING_DATASOURCE_USERNAME=root
SPRING_DATASOURCE_PASSWORD=
SPRING_DATASOURCE_DRIVER=com.mysql.cj.jdbc.Driver
SPRING_JPA_DDL_AUTO=update
SPRING_HIBERNATE_DIALECT=org.hibernate.dialect.MySQL8Dialect

# File upload / image storage
IMAGE_FOLDER_PATH=/app/images
SPRING_MULTIPART_MAX_FILE_SIZE=10MB
SPRING_MULTIPART_MAX_REQUEST_SIZE=10MB

# Docker Hub (used locally by the build script) - do NOT commit real secrets
# DOCKERHUB_PASS can be used by the local build_and_push.ps1 for non-interactive login
DOCKERHUB_USER=your-dockerhub-username
DOCKERHUB_PASS=your-dockerhub-password

# GitHub Actions secrets (set in repo settings, NOT in this file)
# DOCKERHUB_USERNAME -> Docker Hub username
# DOCKERHUB_TOKEN -> Docker Hub access token or password
